import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Importe o arquivo CSS global
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
